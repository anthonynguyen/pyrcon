# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py37,flake8,linters

[testenv]
# install pytest in the virtualenv where commands will be executed
# deps = pytest
commands =
#    # NOTE: you can run any command line tool here - not just tests
#    pytest
     python -m unittest
